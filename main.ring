# The Main File
load "package.ring"
load "lib.ring"

func main
    ? "╔═══════════════════════════════════════╗"
    ? "║       Ring Markdown Extension         ║"
    ? "║           Version " + aPackageInfo[:version] + "               ║"
    ? "║    	  Author: ysdragon              ║"
    ? "║      https://github.com/ysdragon      ║"
    ? "╚═══════════════════════════════════════╝"